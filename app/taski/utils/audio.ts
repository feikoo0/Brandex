// Centralized audio utility - All sounds disabled globally per user request
export const playSound = (_type?: 'click' | 'whoosh' | 'pop' | 'tick' | 'keypress' | 'task-click' | 'trash') => {
  return;
};
